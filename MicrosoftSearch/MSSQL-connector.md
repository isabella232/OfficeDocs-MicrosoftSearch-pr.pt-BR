---
title: Microsoft SQL Connector para pesquisa da Microsoft
ms.author: mounika.narayanan
author: monaray
manager: mnirkhe
ms.audience: Admin
ms.topic: article
ms.service: mssearch
localization_priority: Normal
search.appverid:
- BFB160
- MET150
- MOE150
description: Configurar o Microsoft SQL Connector para o Microsoft Search.
ms.openlocfilehash: c31399e65bd4bfc154d10d2e6057fa23d11f030d
ms.sourcegitcommit: ef1eb2bdf31dccd34f0fdc4aa7a0841ebd44f211
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/02/2019
ms.locfileid: "39663149"
---
# <a name="microsoft-sql-server-connector"></a><span data-ttu-id="75a1e-103">Microsoft SQL Server Connector</span><span class="sxs-lookup"><span data-stu-id="75a1e-103">Microsoft SQL server connector</span></span>

<span data-ttu-id="75a1e-104">Com o Microsoft SQL Server Connector, sua organização pode descobrir e indexar dados de um banco de dados do SQL Server local.</span><span class="sxs-lookup"><span data-stu-id="75a1e-104">With a Microsoft SQL server connector, your organization can discover and index data from an on-premises SQL Server database.</span></span> <span data-ttu-id="75a1e-105">O conector indexa o conteúdo especificado na pesquisa da Microsoft.</span><span class="sxs-lookup"><span data-stu-id="75a1e-105">The connector indexes specified content into Microsoft Search.</span></span> <span data-ttu-id="75a1e-106">Para manter o índice atualizado com os dados de origem, ele oferece suporte a rastreamentos completos e incrementais periódicos.</span><span class="sxs-lookup"><span data-stu-id="75a1e-106">To keep the index up to date with source data, it supports periodic full and incremental crawls.</span></span> <span data-ttu-id="75a1e-107">Com o SQL Server Connector, você também pode restringir o acesso aos resultados da pesquisa para determinados usuários.</span><span class="sxs-lookup"><span data-stu-id="75a1e-107">With the SQL Server connector, you can also restrict access to search results for certain users.</span></span>

<span data-ttu-id="75a1e-108">Este artigo é para os administradores do Microsoft 365 ou qualquer pessoa que configure, execute e monitore um Microsoft SQL Server Connector.</span><span class="sxs-lookup"><span data-stu-id="75a1e-108">This article is for Microsoft 365 administrators or anyone who configures, runs, and monitors a Microsoft SQL server connector.</span></span> <span data-ttu-id="75a1e-109">Ele explica como configurar seus recursos de conector e conector, limitações e técnicas de solução de problemas.</span><span class="sxs-lookup"><span data-stu-id="75a1e-109">It explains how to configure your connector and connector capabilities, limitations, and troubleshooting techniques.</span></span>

## <a name="install-a-data-gateway"></a><span data-ttu-id="75a1e-110">Instalar um data gateway</span><span class="sxs-lookup"><span data-stu-id="75a1e-110">Install a data gateway</span></span>
<span data-ttu-id="75a1e-111">Para acessar seus dados de terceiros, você deve instalar e configurar um gateway do Microsoft Power BI.</span><span class="sxs-lookup"><span data-stu-id="75a1e-111">In order to access your third-party data, you must install and configure a Microsoft Power BI gateway.</span></span> <span data-ttu-id="75a1e-112">Consulte [Install and local gateway on-premises](https://docs.microsoft.com/data-integration/gateway/service-gateway-install) para saber mais.</span><span class="sxs-lookup"><span data-stu-id="75a1e-112">See [Install and on-premises gateway](https://docs.microsoft.com/data-integration/gateway/service-gateway-install) to learn more.</span></span>  

## <a name="connect-to-a-data-source"></a><span data-ttu-id="75a1e-113">Conectar-se a uma fonte de dados</span><span class="sxs-lookup"><span data-stu-id="75a1e-113">Connect to a data source</span></span>
<span data-ttu-id="75a1e-114">Para conectar o Microsoft SQL Server Connector a uma fonte de dados, você deve configurar o servidor de banco de dados que deseja rastrear e o gateway local.</span><span class="sxs-lookup"><span data-stu-id="75a1e-114">To connect your Microsoft SQL server connector to a data source, you must configure the database server you want crawled and the on-premises gateway.</span></span> <span data-ttu-id="75a1e-115">Em seguida, você pode se conectar ao banco de dados com o método de autenticação necessário.</span><span class="sxs-lookup"><span data-stu-id="75a1e-115">You can then connect to the database with the required authentication method.</span></span>

> [!NOTE]
> <span data-ttu-id="75a1e-116">Seu banco de dados deve executar o SQL Server versão 2008 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="75a1e-116">Your database must run SQL server version 2008 or later.</span></span>

<span data-ttu-id="75a1e-117">Para pesquisar seu conteúdo de banco de dados, você deve especificar consultas SQL ao configurar o conector.</span><span class="sxs-lookup"><span data-stu-id="75a1e-117">To search your database content, you must specify SQL queries when you configure the connector.</span></span> <span data-ttu-id="75a1e-118">Essas consultas SQL precisam nomear todas as colunas do banco de dados que você deseja indexar (ou seja, propriedades de origem), incluindo quaisquer junções SQL que precisam ser executadas para obter todas as colunas.</span><span class="sxs-lookup"><span data-stu-id="75a1e-118">These SQL queries need to name all the database columns that you want to index (i.e. source properties), including any SQL joins that need to be performed to get all the columns.</span></span> <span data-ttu-id="75a1e-119">Para restringir o acesso aos resultados da pesquisa, você deve especificar listas de controle de acesso (ACLs) com consultas SQL ao configurar o Microsoft SQL Server Connector.</span><span class="sxs-lookup"><span data-stu-id="75a1e-119">To restrict access to search results, you must specify Access Control Lists (ACLs) with SQL queries when you configure the Microsoft SQL server connector.</span></span>

## <a name="full-crawl-required"></a><span data-ttu-id="75a1e-120">Rastreamento completo (obrigatório)</span><span class="sxs-lookup"><span data-stu-id="75a1e-120">Full crawl (Required)</span></span>
<span data-ttu-id="75a1e-121">Nesta etapa, você configura a consulta SQL que executa um rastreamento completo do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="75a1e-121">In this step, you configure the SQL query that runs a full crawl of the database.</span></span> <span data-ttu-id="75a1e-122">O rastreamento completo seleciona todas as colunas ou propriedades que você deseja tornar **consultáveis**, **pesquisáveis**ou **recuperáveis**.</span><span class="sxs-lookup"><span data-stu-id="75a1e-122">The full crawl selects all the columns or properties you want to be made **queryable**, **searchable**, or **retrievable**.</span></span> <span data-ttu-id="75a1e-123">Você também pode especificar colunas ACL para restringir o acesso de resultados de pesquisa a usuários ou grupos específicos.</span><span class="sxs-lookup"><span data-stu-id="75a1e-123">You can also specify ACL columns to restrict access of search results to specific users or groups.</span></span>

> [!Tip]
> <span data-ttu-id="75a1e-124">Para obter todas as colunas necessárias, você pode ingressar em várias tabelas.</span><span class="sxs-lookup"><span data-stu-id="75a1e-124">To get all the columns that you need, you can join multiple tables.</span></span>

![Script mostrando as propriedades Ordertable e AclTable com exemplo](media/MSSQL-fullcrawl.png)

### <a name="select-data-columns-required-and-acl-columns-optional"></a><span data-ttu-id="75a1e-126">Selecionar colunas de dados (obrigatórias) e colunas ACL (opcional)</span><span class="sxs-lookup"><span data-stu-id="75a1e-126">Select data columns (Required) and ACL columns (Optional)</span></span>
<span data-ttu-id="75a1e-127">O exemplo demonstra a seleção de cinco colunas de dados que armazenam os dados da pesquisa: NúmeroDoPedido, OrderTitle, OrderDesc, CreatedDateTime e IsDeleted.</span><span class="sxs-lookup"><span data-stu-id="75a1e-127">The example demonstrates selection of five data columns that hold the data for the search: OrderId, OrderTitle, OrderDesc, CreatedDateTime, and IsDeleted.</span></span> <span data-ttu-id="75a1e-128">Para definir as permissões de exibição para cada linha de dados, você pode, opcionalmente, selecionar essas colunas de ACL: AllowedUsers, AllowedGroups, DeniedUsers e DeniedGroups.</span><span class="sxs-lookup"><span data-stu-id="75a1e-128">To set view permissions for each row of data, you can optionally select these ACL columns: AllowedUsers, AllowedGroups, DeniedUsers, and DeniedGroups.</span></span> <span data-ttu-id="75a1e-129">Todas essas colunas de dados podem se tornar **consultáveis**, **pesquisáveis**ou **recuperáveis**.</span><span class="sxs-lookup"><span data-stu-id="75a1e-129">All these data columns can be made **queryable**, **searchable**, or **retrievable**.</span></span>

<span data-ttu-id="75a1e-130">Selecione colunas de dados, conforme mostrado neste exemplo de consulta:`SELECT OrderId, OrderTitle, OrderDesc, AllowedUsers, AllowedGroups, DeniedUsers, DeniedGroups, CreatedDateTime, IsDeleted`</span><span class="sxs-lookup"><span data-stu-id="75a1e-130">Select data columns as shown in this example query: `SELECT OrderId, OrderTitle, OrderDesc, AllowedUsers, AllowedGroups, DeniedUsers, DeniedGroups, CreatedDateTime, IsDeleted`</span></span>
 
<span data-ttu-id="75a1e-131">Para gerenciar o acesso aos resultados da pesquisa, você pode especificar uma ou mais colunas ACL na consulta.</span><span class="sxs-lookup"><span data-stu-id="75a1e-131">To manage access to the search results, you can specify one or more ACL columns in the query.</span></span> <span data-ttu-id="75a1e-132">O conector SQL permite controlar o acesso no nível de registro.</span><span class="sxs-lookup"><span data-stu-id="75a1e-132">The SQL connector allows you to control access at per record level.</span></span> <span data-ttu-id="75a1e-133">Você pode optar por ter o mesmo controle de acesso para todos os registros em uma tabela.</span><span class="sxs-lookup"><span data-stu-id="75a1e-133">You can choose to have the same access control for all records in a table.</span></span> <span data-ttu-id="75a1e-134">Se as informações de ACL forem armazenadas em uma tabela separada, talvez seja necessário fazer uma junção com essas tabelas em sua consulta.</span><span class="sxs-lookup"><span data-stu-id="75a1e-134">If the ACL information is stored in a separate table, you might have to do a join with those tables in your query.</span></span>

<span data-ttu-id="75a1e-135">O uso de cada uma das colunas ACL na consulta acima é descrita abaixo.</span><span class="sxs-lookup"><span data-stu-id="75a1e-135">The use of each of the ACL columns in the above query is described below.</span></span> <span data-ttu-id="75a1e-136">A lista a seguir explica os quatro **mecanismos de controle de acesso**.</span><span class="sxs-lookup"><span data-stu-id="75a1e-136">The following list explains the 4 **access control mechanisms**.</span></span> 
* <span data-ttu-id="75a1e-137">**AllowedUsers**: especifica a lista de IDs de usuário que poderão acessar os resultados da pesquisa.</span><span class="sxs-lookup"><span data-stu-id="75a1e-137">**AllowedUsers**: This specifies the list of user IDs who will be able to access the search results.</span></span> <span data-ttu-id="75a1e-138">No exemplo a seguir, a lista de usuários: john@contoso.com, keith@contoso.com e lisa@contoso.com teria apenas acesso a um registro com NúmeroDoPedido = 12.</span><span class="sxs-lookup"><span data-stu-id="75a1e-138">In the following example, list of users: john@contoso.com , keith@contoso.com, and lisa@contoso.com would only have access to a record with OrderId = 12.</span></span> 
* <span data-ttu-id="75a1e-139">**AllowedGroups**: especifica o grupo de usuários que poderá acessar os resultados da pesquisa.</span><span class="sxs-lookup"><span data-stu-id="75a1e-139">**AllowedGroups**: This specifies the group of users who will be able to access the search results.</span></span> <span data-ttu-id="75a1e-140">No exemplo a seguir, o grupo sales-team@contoso.com só teria acesso ao registro com NúmeroDoPedido = 12.</span><span class="sxs-lookup"><span data-stu-id="75a1e-140">In the following example, group sales-team@contoso.com would only have access to record with OrderId = 12.</span></span>
* <span data-ttu-id="75a1e-141">**DeniedUsers**: especifica a lista de usuários que **não** têm acesso aos resultados da pesquisa.</span><span class="sxs-lookup"><span data-stu-id="75a1e-141">**DeniedUsers**: This specifies the list of users who do **not** have access to the search results.</span></span> <span data-ttu-id="75a1e-142">No exemplo a seguir, os usuários john@contoso.com e keith@contoso.com não têm acesso ao registro com NúmeroDoPedido = 13, enquanto todas as outras pessoas têm acesso a esse registro.</span><span class="sxs-lookup"><span data-stu-id="75a1e-142">In the following example, users john@contoso.com and keith@contoso.com do not have access to record with OrderId = 13, whereas everyone else has access to this record.</span></span> 
* <span data-ttu-id="75a1e-143">**DeniedGroups**: especifica o grupo de usuários que **não** têm acesso aos resultados da pesquisa.</span><span class="sxs-lookup"><span data-stu-id="75a1e-143">**DeniedGroups**: This specifies the group of users who do **not** have access to the search results.</span></span> <span data-ttu-id="75a1e-144">No exemplo a seguir, os grupos engg-team@contoso.com e pm-team@contoso.com não têm acesso ao registro com NúmeroDoPedido = 15, enquanto todas as outras pessoas têm acesso a esse registro.</span><span class="sxs-lookup"><span data-stu-id="75a1e-144">In the following example, groups engg-team@contoso.com and pm-team@contoso.com do not have access to record with OrderId = 15, whereas everyone else has access to this record.</span></span>  

![](media/MSSQL-ACL1.png)

### <a name="watermark-required"></a><span data-ttu-id="75a1e-145">Marca d' água (obrigatório)</span><span class="sxs-lookup"><span data-stu-id="75a1e-145">Watermark (Required)</span></span>
<span data-ttu-id="75a1e-146">Para evitar a sobrecarga do banco de dados, o conector batche e retoma consultas de rastreamento completo com uma coluna de marca d' água de rastreamento completo.</span><span class="sxs-lookup"><span data-stu-id="75a1e-146">To prevent overloading the database, the connector batches and resumes full-crawl queries with a full-crawl watermark column.</span></span> <span data-ttu-id="75a1e-147">Usando o valor da coluna watermark, cada lote subsequente é buscado e a consulta é retomada do último ponto de verificação.</span><span class="sxs-lookup"><span data-stu-id="75a1e-147">By using the value of the watermark column, each subsequent batch is fetched, and querying is resumed from the last checkpoint.</span></span> <span data-ttu-id="75a1e-148">Essencialmente, este é um mecanismo para controlar a atualização de dados para rastreamentos completos.</span><span class="sxs-lookup"><span data-stu-id="75a1e-148">Essentially this is a mechanism to control data refresh for full crawls.</span></span>

<span data-ttu-id="75a1e-149">Criar trechos de consulta para marcas d' água, conforme mostrado nestes exemplos:</span><span class="sxs-lookup"><span data-stu-id="75a1e-149">Create query snippets for watermarks as shown in these examples:</span></span>
* <span data-ttu-id="75a1e-150">`WHERE (CreatedDateTime > @watermark)`.</span><span class="sxs-lookup"><span data-stu-id="75a1e-150"></span></span> <span data-ttu-id="75a1e-151">Cite o nome da coluna da marca d' água `@watermark`com a palavra-chave reservada.</span><span class="sxs-lookup"><span data-stu-id="75a1e-151">Cite the watermark column name with the reserved keyword `@watermark`.</span></span> <span data-ttu-id="75a1e-152">Se a ordem de classificação da coluna de marca d' água for crescente `>`, use; caso contrário, `<`use.</span><span class="sxs-lookup"><span data-stu-id="75a1e-152">If the sort order of the watermark column is ascending, use `>`; otherwise, use `<`.</span></span>
* <span data-ttu-id="75a1e-153">`ORDER BY CreatedDateTime ASC`.</span><span class="sxs-lookup"><span data-stu-id="75a1e-153"></span></span> <span data-ttu-id="75a1e-154">Classifique na coluna de marca d' água em ordem crescente ou decrescente.</span><span class="sxs-lookup"><span data-stu-id="75a1e-154">Sort on the watermark column in ascending or descending order.</span></span>

<span data-ttu-id="75a1e-155">Na configuração mostrada na imagem a seguir, `CreatedDateTime` é a coluna de marca d' água selecionada.</span><span class="sxs-lookup"><span data-stu-id="75a1e-155">In the configuration shown in the following image, `CreatedDateTime` is the selected watermark column.</span></span> <span data-ttu-id="75a1e-156">Para buscar o primeiro lote de linhas, especifique o tipo de dados da coluna de marca d' água.</span><span class="sxs-lookup"><span data-stu-id="75a1e-156">To fetch the first batch of rows, specify the data type of the watermark column.</span></span> <span data-ttu-id="75a1e-157">Nesse caso, o tipo de dados é `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="75a1e-157">In this case, the data type is `DateTime`.</span></span>

![](media/MSSQL-watermark.png)

<span data-ttu-id="75a1e-158">A primeira consulta busca a **primeira quantidade de** linhas usando: "CreatedDateTime > 1 de janeiro de 1753 00:00:00" (o valor mín do tipo de dados DateTime).</span><span class="sxs-lookup"><span data-stu-id="75a1e-158">The first query fetches the first **N** amount of rows by using: "CreatedDateTime > January 1, 1753 00:00:00" (min value of DateTime data type).</span></span> <span data-ttu-id="75a1e-159">Após o primeiro lote ser obtido, o valor mais alto de `CreatedDateTime` retornado no lote é salvo como o ponto de verificação se as linhas forem classificadas em ordem crescente.</span><span class="sxs-lookup"><span data-stu-id="75a1e-159">After the first batch is fetched, the highest value of `CreatedDateTime` returned in the batch is saved as the checkpoint if the rows are sorted in ascending order.</span></span> <span data-ttu-id="75a1e-160">Um exemplo é 1 de março de 2019 03:00:00.</span><span class="sxs-lookup"><span data-stu-id="75a1e-160">An example is March 1, 2019 03:00:00.</span></span> <span data-ttu-id="75a1e-161">Em seguida, o próximo lote de **N** linhas é buscado usando "CreatedDateTime > 1 de março de 2019 03:00:00" na consulta.</span><span class="sxs-lookup"><span data-stu-id="75a1e-161">Then the next batch of **N** rows is fetched by using "CreatedDateTime > March 1, 2019 03:00:00" in the query.</span></span>

### <a name="skipping-soft-deleted-rows-optional"></a><span data-ttu-id="75a1e-162">Ignorando linhas excluídas por software (opcional)</span><span class="sxs-lookup"><span data-stu-id="75a1e-162">Skipping soft-deleted rows (Optional)</span></span>
<span data-ttu-id="75a1e-163">Para excluir as linhas excluídas de forma reversível em seu banco de dados, especifique o nome da coluna de exclusão reversível e o valor que indica que a linha foi excluída.</span><span class="sxs-lookup"><span data-stu-id="75a1e-163">To exclude soft-deleted rows in your database from being indexed, specify the soft-delete column name and value that indicates the row is deleted.</span></span>

![Configurações de exclusão reversível: "coluna de exclusão reversível" e "valor da coluna de exclusão reversível que indica uma linha excluída"](media/MSSQL-softdelete.png)

### <a name="full-crawl-manage-search-permissions"></a><span data-ttu-id="75a1e-165">Rastreamento completo: gerenciar permissões de pesquisa</span><span class="sxs-lookup"><span data-stu-id="75a1e-165">Full crawl: Manage search permissions</span></span>
<span data-ttu-id="75a1e-166">Clique em **gerenciar permissões** para selecionar as várias colunas de controle de acesso (ACL) que especificam o mecanismo de controle de acesso.</span><span class="sxs-lookup"><span data-stu-id="75a1e-166">Click **Manage permissions** to select the various access control (ACL) columns which specify the access control mechanism.</span></span> <span data-ttu-id="75a1e-167">Selecione o nome da coluna que você especificou na consulta SQL de rastreamento completo.</span><span class="sxs-lookup"><span data-stu-id="75a1e-167">Select the column name you specified in the full crawl SQL query.</span></span> 

<span data-ttu-id="75a1e-168">Cada uma das colunas ACL deve ser uma coluna de vários valores.</span><span class="sxs-lookup"><span data-stu-id="75a1e-168">Each of the ACL columns is expected to be a multi-valued column.</span></span> <span data-ttu-id="75a1e-169">Esses valores de ID múltiplos podem ser separados por separadores como ponto-e-vírgula (;), vírgula (,) e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="75a1e-169">These multiple ID values can be separated by separators such as semicolon (;), comma (,), and so on.</span></span> <span data-ttu-id="75a1e-170">Você precisa especificar esse separador no campo **separador de valores** .</span><span class="sxs-lookup"><span data-stu-id="75a1e-170">You need to specify this separator in the **value separator** field.</span></span>
 
<span data-ttu-id="75a1e-171">Os seguintes tipos de ID têm suporte para o uso como ACLs:</span><span class="sxs-lookup"><span data-stu-id="75a1e-171">The following ID types are supported for using as ACLs:</span></span> 
* <span data-ttu-id="75a1e-172">**Nome principal do usuário (UPN)**: um nome de usuário principal (UPN) é o nome de um usuário do sistema em um formato de endereço de email.</span><span class="sxs-lookup"><span data-stu-id="75a1e-172">**User Principal Name (UPN)**: A User Principal Name (UPN) is the name of a system user in an email address format.</span></span> <span data-ttu-id="75a1e-173">Um UPN (por exemplo: john.doe@domain.com) consiste do nome de usuário (nome de logon), separador (o símbolo @) e nome de domínio (sufixo UPN).</span><span class="sxs-lookup"><span data-stu-id="75a1e-173">A UPN (for example: john.doe@domain.com) consists of the username (logon name), separator (the @ symbol), and domain name (UPN suffix).</span></span> 
* <span data-ttu-id="75a1e-174">**ID do Azure Active Directory (AAD)**: no AAD, cada usuário ou grupo tem uma ID de objeto que se parece com algo como ' e0d3ad3d-0000-1111-2222-3c5f5c52ab9b '</span><span class="sxs-lookup"><span data-stu-id="75a1e-174">**Azure Active Directory (AAD) ID**: In AAD, every user or group has an object ID which looks something like ‘e0d3ad3d-0000-1111-2222-3c5f5c52ab9b’</span></span> 
* <span data-ttu-id="75a1e-175">**ID de segurança do Active Directory (AD)**: em uma instalação do AD local, todos os usuários e grupos têm um identificador de segurança imutável e exclusivo que se parece com o valor-1-5-21-3878594291-2115959936-132693609-65242.</span><span class="sxs-lookup"><span data-stu-id="75a1e-175">**Active Directory (AD) Security ID**: In an on-premises AD setup, every user and group has an immutable, unique security identifier which looks something like ‘S-1-5-21-3878594291-2115959936-132693609-65242.’</span></span>

![](media/MSSQL-ACL2.png)

## <a name="incremental-crawl-optional"></a><span data-ttu-id="75a1e-176">Rastreamento incremental (opcional)</span><span class="sxs-lookup"><span data-stu-id="75a1e-176">Incremental crawl (Optional)</span></span>
<span data-ttu-id="75a1e-177">Nesta etapa opcional, forneça uma consulta SQL para executar um rastreamento incremental do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="75a1e-177">In this optional step, provide a SQL query to run an incremental crawl of the database.</span></span> <span data-ttu-id="75a1e-178">Com essa consulta, o Microsoft SQL Server Connector faz qualquer alteração nos dados desde o último rastreamento incremental.</span><span class="sxs-lookup"><span data-stu-id="75a1e-178">With this query, the Microsoft SQL server connector makes any changes to the data since the last incremental crawl.</span></span> <span data-ttu-id="75a1e-179">Como no rastreamento completo, selecione todas as colunas que você deseja tornar **consultáveis**, **pesquisáveis**ou **recuperáveis**.</span><span class="sxs-lookup"><span data-stu-id="75a1e-179">As in the full crawl, select all columns that you want to be made **queryable**, **searchable**, or **retrievable**.</span></span> <span data-ttu-id="75a1e-180">Especifique o mesmo conjunto de colunas de ACL que você especificou na consulta de rastreamento completo.</span><span class="sxs-lookup"><span data-stu-id="75a1e-180">Specify the same set of ACL columns that you specified in the full crawl query.</span></span>

<span data-ttu-id="75a1e-181">Os componentes da imagem a seguir são semelhantes aos componentes de rastreamento completo com uma exceção.</span><span class="sxs-lookup"><span data-stu-id="75a1e-181">The components in the following image resemble the full crawl components with one exception.</span></span> <span data-ttu-id="75a1e-182">Nesse caso, "ModifiedDatetime" é a coluna de marca d' água selecionada.</span><span class="sxs-lookup"><span data-stu-id="75a1e-182">In this case, "ModifiedDateTime" is the selected watermark column.</span></span> <span data-ttu-id="75a1e-183">Revise as [etapas de rastreamento completo](#full-crawl-required) para saber como escrever sua consulta de rastreamento incremental e consulte a imagem a seguir como um exemplo.</span><span class="sxs-lookup"><span data-stu-id="75a1e-183">Review the [full crawl steps](#full-crawl-required) to learn how to write your incremental crawl query and see the following image as an example.</span></span>

![O script de rastreamento incremental mostrando as propriedades Ordertable, AclTable e example que podem ser usadas.](media/MSSQL-incrcrawl.png)

## <a name="manage-search-permissions"></a><span data-ttu-id="75a1e-185">Gerenciar permissões de pesquisa</span><span class="sxs-lookup"><span data-stu-id="75a1e-185">Manage search permissions</span></span> 
<span data-ttu-id="75a1e-186">Você pode optar por usar as [ACLs especificadas na tela de rastreamento completo](#full-crawl-manage-search-permissions) ou pode substituí-las para tornar o conteúdo visível para todos.</span><span class="sxs-lookup"><span data-stu-id="75a1e-186">You can choose to use the [ACLs specified in the full crawl screen](#full-crawl-manage-search-permissions) or you can override them to make your content visible to everyone.</span></span>

## <a name="limitations"></a><span data-ttu-id="75a1e-187">Limitações</span><span class="sxs-lookup"><span data-stu-id="75a1e-187">Limitations</span></span>
<span data-ttu-id="75a1e-188">O Microsoft SQL Server Connector tem essas limitações na versão prévia:</span><span class="sxs-lookup"><span data-stu-id="75a1e-188">The Microsoft SQL server connector has these limitations in the preview release:</span></span>
* <span data-ttu-id="75a1e-189">O banco de dados local deve executar o SQL Server versão 2008 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="75a1e-189">The on-premises database must run SQL server version 2008 or later.</span></span>
* <span data-ttu-id="75a1e-190">As ACLs só são compatíveis com o uso de um UPN (nome principal de usuário), do Azure Active Directory (Azure AD) ou da segurança do Active Directory.</span><span class="sxs-lookup"><span data-stu-id="75a1e-190">ACLs are only supported by using a User Principal Name (UPN), Azure Active Directory (Azure AD), or Active Directory Security.</span></span> 
* <span data-ttu-id="75a1e-191">Não há suporte para indexação de conteúdo avançado nas colunas do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="75a1e-191">Indexing rich content inside database columns is not supported.</span></span> <span data-ttu-id="75a1e-192">Exemplos desse tipo de conteúdo são HTML, JSON, XML, BLOBs e análises de documento que existem como links dentro das colunas do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="75a1e-192">Examples of such content are HTML, JSON, XML, blobs, and document parsings that exist as links inside the database columns.</span></span>